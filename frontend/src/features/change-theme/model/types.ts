
// в будущем можно будет добавить больше тем
export type ThemeMode = "dark" | "light" | "system"

// не инициализатор новых тем, необходимо только для switch-toggle
export type ToggleTheme = "dark" | "light"