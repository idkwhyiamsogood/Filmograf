import React from 'react';
import type { Collection } from '../model/types';

interface Props {
  collection: Collection;
}

export const ColllectionFull: React.FC<Props> = ({ collection }) => {
  return (
    <div>
      
    </div>
  );
};
